import SideBar from '../../components/SideBar';
import { GalleryStyles } from './styles';

const Gallery = () => {
  return (
    <GalleryStyles>
      <SideBar /> Gallery
    </GalleryStyles>
  );
};

export default Gallery;
